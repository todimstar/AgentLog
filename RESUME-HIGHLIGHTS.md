# 简历亮点句（可直接粘贴）· 逐课累积

> **这份文件的定位**（主人 2026-08-17 定的新标准）：
> 每课收尾必须产出**一句以上可以直接写进简历的亮点描述**，
> 而所有代码讲解、设计讲解都**围绕这几句展开**——其余部分一笔带过。
>
> 理由：项目是要挂到简历上的。面试官**只会照着简历上的点问**，
> 所以「哪几句能上简历」就是这一课**唯一的验收标准**。
>
> **怎么用**：
> 1. 简历上写「亮点句」那一栏（**动词开头 + 技术名词 + 可量化结果**）；
> 2. 面试官照着问 → 你按「他会怎么反问」那一列展开；
> 3. 深挖到细节时 → 翻 `INTERVIEW-STORIES.md` 对应故事（那里有完整的排查过程与命令）。

---

## L18 · Retry 与 Terminate（人的决策链）

### 🥇 亮点句 1（技术深度最硬，建议放简历最前）

> **定位并修复一起 MySQL 死锁**：查明 ORM 全字段更新（MyBatis-Plus `updateById`）会因**外键列触发父表 S 锁**，
> 与 Spring Modulith **异步审计写入**形成锁环；改为**精准条件更新** + 移除流水表外键约束（`V016` 迁移），
> 集成测试由偶发失败恢复为稳定全绿。

| 面试官会怎么反问 | 你答什么（核心句） | 深挖去哪 |
|---|---|---|
| 你怎么发现是死锁的？ | 报错是 `DeadlockLoserDataAccessException`，`Failures: 0 / Errors: 2` —— **0 个断言失败说明业务逻辑没错，是别的东西炸了** | 故事 38 §2 |
| 一条 UPDATE 怎么会死锁？ | 因为 `updateById` **SET 全部列**，其中三列是外键——**外键不是免费的完整性检查，它是隐式的锁**，会给父表行加 S 锁 | 故事 38 §3 |
| 那另一个事务是谁？ | 我新加的一行 `publishEvent`。它触发的 `@ApplicationModuleListener` 在**另一个线程、另一个独立事务**里 INSERT 审计表，而那张表有 **4 个外键** | 故事 38 §4 |
| 为什么不加重试就完事？ | 那是治症状。而且**修完只减轻不消失（2→1）恰恰是最有价值的信号**：方向对，但根因没挖到底 | 故事 38 §3 结尾 |
| 删外键不怕数据不一致？ | 判据是**外键防的那件事在这张表上会不会发生**——审计流水的每一列都由我们自己的代码写、父行从不物理删除（都是墓碑软删）⇒ 防的事不会发生，成本每次都付 | 故事 38 §6 |
| 生产上怎么确认锁环？ | `SHOW ENGINE INNODB STATUS` 看 `LATEST DETECTED DEADLOCK`；偶发问题开 `innodb_print_all_deadlocks`；8.0 用 `performance_schema.data_locks` / `sys.innodb_lock_waits` | 故事 38 §7 |
| 为什么三课都没炸？ | **并发缺陷的暴露需要压力，而压力常常由一个看似无关的新功能提供**——审计事件原来很稀疏，我把它加到了高频路径上 | 故事 38 §5 |

**三条可以直接背的判据**：
1. `updateById` 的代价不是"多写几列"，是**"多锁几张表"**；
2. **流水表不建外键**；
3. **外键不是免费的完整性，它是隐式的锁**——你以为在声明约束，其实在声明加锁顺序。

---

### 🥈 亮点句 2（业务 + 并发，最能讲出"我设计了什么"）

> **设计并实现多 AI 协作的失败恢复链路**（重试 / 终止 / 凭证重签）：以**带条件的原子 UPDATE 作闸门**
> 保证并发与重复点击下语义正确，用**递归 CTE 沿因果链**恢复整条被阻塞的后继任务；
> **8 线程并发测试**验证同一时刻仅一个请求生效。

| 面试官会怎么反问 | 你答什么（核心句） | 深挖去哪 |
|---|---|---|
| 为什么不做自动重试？ | 服务端与 AI 之间是**「拉」不是「推」**——它无法叫醒一个已关闭的对话。自动重试只会「改回可写 → 再超时 → 再重试」**死循环**。判据：**这一步需要的信息只存在于系统之外** | 讲义 §1 |
| 闸门是什么？为什么不先查再改？ | 一条 UPDATE 里同时完成检查与动作，`affectedRows` 就是裁决书。分两步会留 **TOCTOU** 缝隙，并发下双双通过 | 故事 39 §0 |
| 为什么用递归 CTE？ | 因果链是链表：第 4 棒的前驱指向第 3 棒，用 `WHERE predecessor = 死票` **只能匹配到直接后继一张**。判据：**同一条链表，唤醒只走一步、冻结/解冻要走到底** | 讲义 §3 |
| 恢复时后继任务应该变成什么状态？ | **「排队中」不是「可写」**——前一棒只是"可以重写了"、还没写完。蓝图把这两步合成了一步，我们改了 | 讲义 §3 |
| 8 线程测什么？为什么不是 2 个？ | 主断言是**同步且确定的**（数成功次数=1），不是去数异步产物。2 线程可能侥幸错开，测不出竞态——**测试数据的形状决定了它能发现什么** | 讲义 §12 |

---

### 🥉 亮点句 3（设计判断力，面试官最容易被打动的一类）

> **区分「幂等键」与「条件更新闸门」的适用边界**并据此选型：前者防请求重发、保证**响应可重放**，
> 后者在**数据库层**防重复执行；为状态迁移类端点省去无效的幂等开销，为内容写入类端点保留完整幂等台账。

| 面试官会怎么反问 | 你答什么（核心句） | 深挖去哪 |
|---|---|---|
| 两个都能防重复，为什么都要？ | **闸门保证数据对，幂等保证调用方知道数据对。** 只有闸门时，"服务端成功了但响应丢了"会让客户端拿到 409、误判为失败 | 故事 39 §2 |
| 怎么判断一个接口要不要幂等？ | **先问「重发会不会造成第二次真实的改变」**。会（INSERT 类）→ 要；不会（状态迁移类）→ 闸门足够 | 故事 39 §3 |
| 浏览器能做幂等吗？ | 能，但只能防"同一页面连点"——**刷新或换标签页就是新 key**。规律：**幂等的适用性取决于客户端能不能跨请求持有同一个 key** | 故事 39 §4 |
| 支付这种怎么做？ | 两个都上，且 key 用**业务单号**充当。判据：**业务本身有天然唯一标识时，那个标识就是最好的幂等键** | 故事 39 §4 |
| 闸门该建在哪张表上？ | 选那个**"能唯一代表这次操作发生过"的对象**——即状态会因此不可逆改变的那一行。进阶：**两个操作的闸门落在不同对象上时，必须有一方把对方的条件纳入自己的 WHERE** | 故事 40 |

---

### 备用（不单独占简历行，用于回答"你的工程习惯"类问题）

> 梳理协作协议 **4 个状态机 27 个状态值**的产生路径，识别并处置 **5 个「值集已定义但无任何代码产生」的死状态**
> 与 **3 处「注释承诺未兑现」的实现缺口**。

一句话核心：**CHECK 值集里有个值，不等于有代码会产生它——值集是承诺，不是实现。**
而最狠的一处是一个**只写不读**的状态字段（写 4 处、读 0 处），
所以它错了两课、两轮测试全绿：**一个从来没被读过的状态，连骗人的机会都没有。**

---

## L01–L17（待整理）

暂以 `INTERVIEW-STORIES.md` 里标了 **🔴 简历主打** 的故事为准：
- 故事 13：为 CLI + AI Agent 设计四层认证体系（L12–L13）
- 其余待按本文件格式回填。
